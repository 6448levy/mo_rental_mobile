<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Permits cleartext (HTTP) traffic to the local dev backend so
  `flutter run` can reach it. Release builds do NOT include this file and remain
  HTTPS-only (cleartext denied by default on Android 9+).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">13.61.185.238</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
